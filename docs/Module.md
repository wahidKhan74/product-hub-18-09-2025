Modules : Modules are reusable block of code in Js file.

Core modules → fs, http, os, path etc.

Local modules → custom business logic, utilities, configs.

Third-party modules → install with npm (like Express).